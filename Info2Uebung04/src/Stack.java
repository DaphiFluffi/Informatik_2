import java.util.LinkedList;

/*public class Stack <E> {
private SinglyLinkedList <E> lst =
			 new SinglyLinkedList < >();
 public void push (E e) {
		 // pushing an element is done by adding the
		 // element at the beginning of the list ( index 0)
		 lst .add (0, e);
		 }
 }*/